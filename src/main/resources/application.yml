spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hospital
    username: postgres
    password: syimyk

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

server:
  port: 8080